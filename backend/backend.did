type Holding = 
 record {
   purchasePrice: float64;
   quantity: float64;
 };
service : {
  addHolding: (text, float64, float64) -> ();
  getHoldings: () -> (vec record {
                            text;
                            Holding;
                          }) query;
}
